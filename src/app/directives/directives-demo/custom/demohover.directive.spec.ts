import { DemohoverDirective } from './demohover.directive';

describe('DemohoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DemohoverDirective();
    expect(directive).toBeTruthy();
  });
});
